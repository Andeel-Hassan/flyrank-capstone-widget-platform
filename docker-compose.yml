services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: widgetapp
      POSTGRES_PASSWORD: widgetapp_pass
      POSTGRES_DB: widget_platform
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
